/// Playback modes for the video player.
enum PlaybackMode {
  /// Normal playback.
  normalPlayback,

  /// Used for condensed audio.
  condensedPlayback,

  /// Automatically pause upon the end of a subtitle.
  autoPausePlayback,
}
